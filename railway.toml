[build]
builder = "nixpacks"

[build.env]
NIXPACKS_INSTALL_CMD = "apt-get update && apt-get install -y python3 python3-pip && pip install -r requirements.txt"
PYTHON_VERSION = "3.10"

[deploy]
startCommand = "python3 -m uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
